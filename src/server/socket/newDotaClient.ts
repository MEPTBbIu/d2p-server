import {TypedEvent} from "../../utils/common/events";
import {Types, cast} from "./socketContract";

//const newDotaClient = new TypedEvent<Types.DotaClientInfo>();

/*export const onNewDotaClient = (cb:(data:Types.DotaClientInfo) => void ) => cast.newDotaClient.on(cb);
export const emitNewDotaClient = (client: Types.DotaClientInfo) => {
	cast.newDotaClient.emit(client);
}*/